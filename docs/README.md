# Documentação AERO RADAR

Consulte `RETROSPECTIVA/` para a retrospectiva consolidada do projeto.

O ZIP de retrospectiva não inclui o arquivo real `server/.env` para evitar exposição de credenciais.
